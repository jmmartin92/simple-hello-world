# contains the pipeline, and depending on jenkins server etc
# Define a pipeline pod that contains many containers to perform various actions ie scan nexus iq, sonar qube
# build image, scan image trivvy or aqua , and perform a release
# 